name: qiita_reader
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '2.18.4'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.5
  url_launcher: ^6.1.7
  html: ^0.15.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
